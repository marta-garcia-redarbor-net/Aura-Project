name: Build and Deploy Aura Containers

on:
  push:
    branches: [release/aura/*]
    paths-ignore:
      - 'infra/**'
      - 'docs/**'
      - '*.md'
  workflow_dispatch:
    inputs:
      imageTag:
        description: 'Docker image tag'
        required: true
        default: 'latest'
      deployToAca:
        description: 'Deploy to ACA after build'
        required: true
        default: true
        type: boolean

env:
  AZURE_RESOURCE_GROUP: aura-rg

permissions:
  id-token: write
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    environment: dev
    strategy:
      matrix:
        include:
          - service: aura-api
            dockerfile: src/Aura.Api/Dockerfile
          - service: aura-ui
            dockerfile: src/Aura.UI/Dockerfile
          - service: aura-workers
            dockerfile: src/Aura.Workers/Dockerfile
    steps:
      - uses: actions/checkout@v4

      - name: Extract version from branch
        shell: bash
        run: |
          BRANCH="${{ github.ref_name }}"
          VERSION="${BRANCH#release/aura/}"
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Login to ACR via az
        run: |
          az acr login --name ${{ secrets.ACR_NAME }}

      - name: Build Image
        run: |
          ACR=${{ secrets.ACR_NAME }}.azurecr.io
          TAG="${{ github.event.inputs.imageTag || github.sha }}"
          docker build \
            -f ${{ matrix.dockerfile }} \
            --build-arg VERSION=$VERSION \
            -t $ACR/${{ matrix.service }}:$TAG \
            -t $ACR/${{ matrix.service }}:latest \
            .

      - name: Push Image
        run: |
          ACR=${{ secrets.ACR_NAME }}.azurecr.io
          docker push --all-tags $ACR/${{ matrix.service }}

  deploy:
    needs: build
    if: ${{ github.event.inputs.deployToAca != 'false' }}
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy to ACA
        run: |
          ACR=${{ secrets.ACR_NAME }}.azurecr.io
          TAG="${{ github.event.inputs.imageTag || github.sha }}"

          az containerapp update \
            --name aura-api-dev \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --image $ACR/aura-api:$TAG

          az containerapp update \
            --name aura-ui-dev \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --image $ACR/aura-ui:$TAG

          az containerapp update \
            --name aura-workers-dev \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --image $ACR/aura-workers:$TAG
