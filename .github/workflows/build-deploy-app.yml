name: Build and Deploy Aura Containers

on:
  push:
    branches: [release/*]
    paths-ignore:
      - 'infra/**'
      - 'docs/**'
      - '*.md'
  workflow_dispatch:
    inputs:
      imageTag:
        description: 'Docker image tag'
        required: true
        default: 'latest'
      deployToAca:
        description: 'Deploy to ACA after build'
        required: true
        default: true
        type: boolean

env:
  AZURE_RESOURCE_GROUP: aura-rg

permissions:
  id-token: write
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - service: aura-api
            dockerfile: src/Aura.Api/Dockerfile
          - service: aura-ui
            dockerfile: src/Aura.UI/Dockerfile
          - service: aura-workers
            dockerfile: src/Aura.Workers/Dockerfile
    steps:
      - uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Login to ACR via az
        run: |
          az acr login --name ${{ secrets.ACR_NAME }}

      - name: Build Image
        run: |
          ACR=${{ secrets.ACR_NAME }}.azurecr.io
          TAG="${{ github.event.inputs.imageTag || github.sha }}"
          docker build \
            -f ${{ matrix.dockerfile }} \
            -t $ACR/${{ matrix.service }}:$TAG \
            -t $ACR/${{ matrix.service }}:latest \
            .

      - name: Push Image
        run: |
          ACR=${{ secrets.ACR_NAME }}.azurecr.io
          docker push --all-tags $ACR/${{ matrix.service }}

  deploy:
    needs: build
    if: ${{ github.event.inputs.deployToAca != 'false' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - service: aura-api
            acaName: aura-api-dev
          - service: aura-ui
            acaName: aura-ui-dev
          - service: aura-workers
            acaName: aura-workers-dev
    steps:
      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Debug Azure context
        run: |
          echo "=== Azure context ==="
          az account show --query "{sub:name, subId:id, tenantId:tenantId}" -o json
          echo "=== Resource group ==="
          az group exists -n ${{ env.AZURE_RESOURCE_GROUP }}
          echo "=== Container apps ==="
          az containerapp list --resource-group ${{ env.AZURE_RESOURCE_GROUP }} --query "[].name" -o tsv

      - name: Deploy to ACA
        run: |
          ACR=${{ secrets.ACR_NAME }}.azurecr.io
          TAG="${{ github.event.inputs.imageTag || github.sha }}"
          echo "Deploying ${{ matrix.service }} to ${{ matrix.acaName }}..."
          az containerapp update \
            --name ${{ matrix.acaName }} \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --image $ACR/${{ matrix.service }}:$TAG
