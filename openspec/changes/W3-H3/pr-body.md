## Summary

- Added per-card high-priority counters next to each source card `new` count (Teams, Outlook, PRs), including accessible `aria-label` text (`{N} high priority`) and neutral badge styling.
- Updated `/top-priority` to a single centered-column feed layout (no right-side panel), while preserving item links and pagination.
- Added targeted TDD coverage for high-priority counting, summary-card high-priority badge rendering, and single-column Top Priority layout behavior.
